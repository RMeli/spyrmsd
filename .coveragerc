[run]
source = spyrmsd
omit = spyrmsd/_version.py

[report]
exclude_lines =
    # Don't complain if tests don't hit defensive assertion code:
    raise AssertionError
    raise NotImplementedError

    # Don't complain if non-runnable code isn't run:
    if 0:
    if __name__ == .__main__.:

    # OpenBabel2
    except ImportError: